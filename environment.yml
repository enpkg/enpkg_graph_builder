name: graph_builder
channels:
  - defaults
  - anaconda
  - conda-forge
  - bioconda
dependencies:
  - python=3.9
  - pip
  - sqlite
  - ipython
  - networkx
  - ipykernel
  - rdflib
  - pandas
  - requests
  - tqdm
  - sqlite
  - pip:
    - pyyaml
prefix: /opt/anaconda3/envs/graph_builder
